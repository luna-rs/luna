# An easier way to configure log4j2 settings. For more info on these settings see 'io.luna.logging.LoggingSettings' or the wiki.

rootLevel = "DEBUG"
formatType = "VERBOSE"
outputType = "MIXED"
activeFileLogs = [ "CONSOLE_ERR" ]
