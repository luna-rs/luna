package world.item.globalSpawn

import api.plugin.dsl.plugin

plugin {
    name = "Global item spawns"
    description =
        """
        A plugin that enables the persistent globally spawning items.
         - Modify at 'data/game/world/spawnItems.json'
        """
    version = "1.0"
    authors += "lare96"
}