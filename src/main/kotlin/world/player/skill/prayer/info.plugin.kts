package world.player.skill.prayer

import api.plugin.plugin

plugin {
    name = "Prayer"
    description =
        """
        A plugin that enables the Prayer skill. This includes
        -> Burying bones
        -> Using bones on an altar for boosted XP
            -> Being able to light the altar for boosted XP
            -> Altar spawn at ;;home
        """
    version = "1.0"
    authors += "lare96"
}