package world.player.skill.prayer

import api.plugin.dsl.plugin

plugin {
    name = "Prayer"
    description =
        """
        A plugin that enables the Prayer skill. This includes
        -> Burying bones
        """
    version = "1.0"
    authors += "lare96"
}