package world.player.item.equipment

import api.plugin.dsl.plugin

plugin {
    name = "Equip/Remove equipment"
    description =
        """
        A plugin that enables players to wear and remove equipment.
        """
    version = "1.0"
    corePlugin = true
    authors += "lare96"
}