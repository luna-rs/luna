package world.player.command

import api.plugin.dsl.plugin

plugin {
    name = "Commands"
    description =
        """
        A plugin that enables most generic commands.
        """
    version = "1.0"
    corePlugin = true
    authors += "lare96"
}