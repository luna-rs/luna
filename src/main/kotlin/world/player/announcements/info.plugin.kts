package world.player.announcements

import api.plugin.dsl.plugin

plugin {
    name = "Repeating announcements"
    description =
        """
        A plugin that sends server-wide announcements at specified intervals.
        """
    version = "1.0"
    authors += "lare96"
}