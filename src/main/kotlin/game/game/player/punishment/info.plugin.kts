package game.player.punishment

import api.plugin.dsl.plugin

plugin {
    name = "Punishments"
    description =
        """
        A plugin that enables mutes, bans, persisted IP bans, and the commands to apply them to players.
        """
    version = "1.0"
    authors += "lare96"
    authors += "searledan"
}
