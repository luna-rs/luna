package game.npc.wanderingBob

import api.plugin.dsl.plugin

plugin {
    name = "Wandering bob"
    description =
        """
        Spawns "Bob" that will randomly wander the entire RS2 world.
        """
    version = "1.0"
    authors += "lare96"
}
