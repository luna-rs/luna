package game.npc.spawn

import api.plugin.dsl.plugin

plugin {
    name = "Generic NPC spawns"
    description =
        """
        A plugin that populates the world with dynamically spawned generic NPCs. This includes
            - Shopkeepers and utility NPCs
            - Special wandering NPCs like bob the Jagex cat
        """
    version = "1.0"
    authors += "hydrozoa"
    authors += "lare96"
}