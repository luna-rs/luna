package game.npc.spawn.location.portSarim

import api.plugin.dsl.plugin

plugin {
    name = "Port Sarim"
    description =
        """
        A plugin that populates Port Sarim with NPC shops.
        """
    version = "1.0"
    authors += "hydrozoa"
}