package game.npc.spawn.location.lumbridge

import api.plugin.dsl.plugin

plugin {
    name = "Lumbridge"
    description =
        """
        A plugin that populates Lumbridge with NPC shops.
        """
    version = "1.0"
    authors += "hydrozoa"
}