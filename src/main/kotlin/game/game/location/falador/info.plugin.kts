package game.location.falador

import api.plugin.dsl.plugin

plugin {
    name = "Falador"
    description =
        """
        A plugin that populates Falador with NPCs.
        """
    version = "1.0"
    authors += "hydrozoa"
}