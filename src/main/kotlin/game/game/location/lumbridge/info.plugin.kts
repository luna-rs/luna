package game.location.lumbridge

import api.plugin.dsl.plugin

plugin {
    name = "Lumbridge Npc Dialogues"
    description =
        """
        A plugin that adds dialogue to NPCs in Lumbridge.
        """
    version = "1.0"
    authors += "hydrozoa"
}