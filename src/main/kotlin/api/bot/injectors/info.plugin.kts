package api.bot.injectors

import api.plugin.dsl.plugin

plugin {
    name = "Bot speech injectors"
    description =
        """
        A plugin that registers all speech injectors.
        """
    version = "1.0"
    authors += "lare96"
}