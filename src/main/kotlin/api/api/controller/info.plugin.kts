package api.controller

import api.plugin.dsl.plugin

plugin {
    name = "Controllers"
    description =
        """
        A plugin that registers location based controllers.
        """
    version = "1.0"
    authors += "lare96"
}
