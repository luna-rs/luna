package api.controller

import api.plugin.plugin

plugin {
    name = "Controllers"
    description =
        """
        A plugin that registers location based controllers.
        """
    version = "1.0"
    corePlugin = true
    authors += "lare96"
}
