package world.player.skill.prayer

import api.bootstrap.plugin

plugin {
    name = "Prayer"
    description = "A plugin that enables burying bones for the Prayer skill."
    version = "1.0"
    authors += "lare96"
}