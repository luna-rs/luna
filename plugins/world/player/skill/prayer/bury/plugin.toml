[metadata]
name = "Prayer"
description = "Adds functionality burying bones.

SUPPORTS:
 -> Burying most bones.
 -> Throttling (can only bury every 1.2 seconds)."
version = "1.0"
authors = [
  "lare96"
]