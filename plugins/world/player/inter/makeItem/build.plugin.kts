package world.player.inter.makeItem

import api.bootstrap.plugin

plugin {
    name = "'Make item' interface"
    description =
        """
        A plugin that enables the use of the 'Make item' interfaces. These interfaces are used in many skills,
        amongst other things.
        """
    version = "1.0"
    authors += "lare96"
}