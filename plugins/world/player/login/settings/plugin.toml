[metadata]
name = "Interface state settings"
description = "Sets interface state settings.

This refers to which buttons are selected in the settings tab, etc."
version = "1.0"
dependencies = [ ]
authors = [
  "lare96"
]