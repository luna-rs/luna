package world.npc.death.dropItems

import api.bootstrap.plugin

plugin {
    name = "NPC death items"
    description =
        """
        A plugin that enables NPCs dropping items when they die.
        """
    version = "1.0"
    authors += "lare96"
}