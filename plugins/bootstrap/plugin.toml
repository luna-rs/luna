[metadata]
name = "Bootstrap"
description = "Acts as an API for all plugins, and allows Scala to interact with Java. This plugin cannot be deselected."
version = "1.0"
dependencies = [
  "fields.scala",
  "functions.scala",
  "event_interception.scala",
  "implicit_classes.scala"
]
authors = [
  "lare96"
]